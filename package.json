{
  "name": "bookyourshow-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && node --max-old-space-size=4096 ./node_modules/npm/bin/npm-cli.js install --prefix frontend",
    "dev": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "build-frontend": "npm run build --prefix frontend",
    "start": "nodemon backend/server.js"
  },
  "devDependencies": {
    "@csstools/normalize.css": "^12.1.1",
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "multer": "^2.0.2",
    "nodemon": "^3.1.10"
  }
}
